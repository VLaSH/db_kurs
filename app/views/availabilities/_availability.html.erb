<tr>
  <td><%= availability.product.name %></td>
  <td><%= availability.amount %></td>
  <td><%= availability.end_date %></td>
  <td><%= availability.price %></td>
  <td><%= link_to 'Show', availability %></td>
  <td><%= link_to 'Edit', edit_availability_path(availability) %></td>
  <td><%= link_to 'Destroy',  { action: :destroy, id: availability.id }, method: :delete, data: { confirm: 'Are you sure?' } %></td>
</tr>
